rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // No client access. Server code uses the Admin SDK, which bypasses rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
